// I don't know mate

/*

class CurrencyHandler {
   constructor(currency) {
      this.currency = currency;
      this.currencyName = p
   }

   add(value) {
      player[this.currency] = this.currency.add(value)
   }
}

window.Currency = {
   seed: new CurrencyHandler(player.seed),
};
*/