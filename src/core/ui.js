import Study from './state/study.js'

const userInterface = document.getElementById('ui-dynamic');
userInterface.style.height = `${250*3}px`;
userInterface.style.width = `${360*2}px`;

console.log(Study([1, 0]))